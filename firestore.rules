rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype mode: Allow all reads and writes for simplicity.
    // In a real production app, you would use Firebase Auth to secure these.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}